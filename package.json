{
  "name": "sistema-controle-taxas-alvaras-full",
  "version": "1.0.0",
  "description": "Sistema de Controle de Taxas e Alvarás (Backend e Frontend)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "NODE_ENV=development nodemon src/index.js",
    "build-frontend": "cd client && npm install --legacy-peer-deps && npm run build",
    "install-backend": "npm install",
    "install-all": "npm install && cd client && npm install --legacy-peer-deps",
    "sequelize": "npx sequelize-cli",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Manus AI",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.12.0",
    "pg-hstore": "^2.3.4",
    "sequelize": "^6.37.3",
    "sequelize-cli": "^6.6.2"
  },
  "devDependencies": {
    "nodemon": "^3.1.4"
  }
}


